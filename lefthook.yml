pre-commit:
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx,json,css}"
      run: bunx biome check --no-errors-on-unmatched {staged_files}

pre-push:
  commands:
    typecheck:
      run: bunx tsc --noEmit
